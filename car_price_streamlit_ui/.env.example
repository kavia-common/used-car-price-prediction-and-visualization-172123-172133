# Environment variables for the React frontend
# Note: CRA exposes only variables prefixed with REACT_APP_
REACT_APP_BACKEND_BASE_URL=http://localhost:3001
# BACKEND_BASE_URL will be ignored by CRA, kept for reference:
# BACKEND_BASE_URL=http://localhost:3001
